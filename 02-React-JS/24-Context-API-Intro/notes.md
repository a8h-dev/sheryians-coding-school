# Props Drilling
app -> navbar -> sidebar -> userprofile
but can't directly (app -> userprofile)

# Context API
- Data Centralization
- ab har ek component usko use and change kar sakta hai

1. Context
2. Provider
3. Use Context